{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @dig/contracts build && pnpm --filter @dig/core build && pnpm --workspace-root run db:generate && pnpm --workspace-root run db:deploy && pnpm --filter @dig/web build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "crons": [{ "path": "/api/contracts/sync", "schedule": "0 20 * * *" }]
}
